/* eslint no-unused-expressions:0 */
import {expect} from 'chai';
import Label from '../src/sui-label';

describe('sui-label component test suite', () => {

  describe('sui-label', () => {
    it('should be loaded properly', () => {
      expect(Label).to.not.be.undefined;
    });
  });

});
